module delic;

public {
  import delic.brainfuck, delic.thue, delic.befunge, delic.set, delic.turimg, delic.selt, delic.rep, 
    delic.doublelang, delic.throughput;
  import delic.common : InterpreterException;
}